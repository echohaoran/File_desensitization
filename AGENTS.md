# 项目协作规范

## 2026-08-28 增量约束

- AI 全文检测必须通过隔离推理子进程调用 Rust 原生 Candle；native 推理失败不得直接终止 Tauri 主进程。
- AI 检测入口位于检测结果板块顶部，显示任务进度，输出只能进入人工确认链路。
- 地址脱敏保留省份和城市，仅替换区县、街道、道路及门牌号片段。

## 必备结构

项目必须保留 `README.md`、`AGENTS.md`、`.gitignore`、`docs/` 和 Git 仓库元数据。

## 文档维护

- `docs/spec.md`：项目规格与架构。
- `docs/handoff.md`：收到“打包”或“转交”指令时，记录项目、Git 状态与开发进度，并更新全部文档。
- `docs/memory.md`：每 10 次对话总结一次增量。
- `docs/troubleshoot.md`：每 15 次对话检查记忆，将重复 3 次以上的问题固定为排障规则。
- `docs/context.md`：每次产生编辑、删除、重命名、重构或新建行为后，记录本次执行内容。
- `docs/todo.md`：创建待办时增量记录并保留时间戳。
- `docs/decisions.md`：记录架构、功能、数据、UI 或大批量代码变更等重大决定。
- `dpcs/smoke_logs.md`：每次冒烟测试后记录范围、结果和环境。

## 开发约束

- 代码更新后同步维护相关文档。
- 浏览器冒烟测试优先使用 ego-browser。
- 真实上传文件不得写入版本控制或作为测试样本；测试样本必须使用虚构数据。

## 下一代架构约束

- 新架构目标为 Vue 3 + Rust + Tauri；在迁移完成前，现有 Vue + FastAPI + Electron 实现作为兼容基线，不得未经确认直接删除。
- 脱敏和反脱敏必须经过人工确认；规则引擎、Rust 引擎和 AI 只能生成候选结果。
- 脱敏页面采用左右预览，右侧仅允许选区操作，不允许直接编辑脱敏正文。
- 输出文件使用随机不可读文档 ID。DOCX/XLSX/PDF 在文件底部追加明文标记页/标记区；JSON、TXT、CSV、Markdown 使用 `.desens-meta` 伴随标记文件，不破坏业务正文结构。
- 跨设备还原必须同时提供脱敏文件和映射数据；完整脱敏库导出包使用 AES-256-GCM 加密，并通过 `.p12` 证书和密码解密。SHA-256 仅用于完整性校验，不得当作加密方案。
- 第一阶段使用版本化 JSON 存储，预留存储抽象；不得直接引入 Redis 作为单机主数据库。
- 模型统一使用 GGUF；模型不随 Tauri 安装包发布，由设置页从魔搭社区/Hugging Face 下载或选择本地路径。
- AI 默认关闭，仅对用户在右侧选中的内容执行检测；AI 结果必须进入人工审核流程。
- 用户标注数据与 LoRA 训练任务必须可导入导出；每个训练任务固定一个经校验的基础 GGUF 模型，训练开始后不可切换。
- 每完成一个设计或实现步骤，必须同步更新 `AGENTS.md` 和受影响的 `docs/*`，并在 `docs/context.md` 记录本次工作。
- 文件标记、映射表、加密导出包等跨模块协议必须先形成版本化设计，再进入代码实现；协议变更必须记录到 `docs/decisions.md`。
- Tauri command 必须使用版本化、可序列化的输入输出类型；Vue 不得直接操作本地路径、映射原文或模型文件。
- 长任务必须通过统一事件报告进度、日志、警告、完成和失败；不得让 UI 轮询临时文件作为主要进度机制。
- 任务管理器只保存任务元数据和状态，不得把敏感原文、mapping 原文或证书秘密写入任务事件。
- 任务进度值必须由 Rust 校验并限制在 0–100；未知任务 ID 必须返回明确错误，不得静默创建任务。
- 任务状态持久化和事件发送失败必须可诊断；事件发送失败不得泄露任务敏感数据，也不得掩盖核心状态写入失败。
- 终态任务（completed/failed/cancelled）不得被普通更新恢复为 queued/running/paused；恢复任务时必须保留原 task ID 和状态。
- command DTO、错误码和任务事件字段一旦冻结，变更必须递增 schema 版本并记录兼容策略；不得复用含义不一致的字段。
- JSON 存储必须使用版本化 envelope、原子写入和受控备份；禁止通过字符串拼接生成 JSON，禁止并发任务直接覆盖同一份数据文件。
- 历史映射、证书材料和训练产物必须与普通规则配置分离保存；日志不得记录其敏感内容。
- 每种文件格式必须通过统一文档适配器处理解析、预览、脱敏输出、标记和还原；不得在 Vue 中按格式散落实现替换逻辑。
- 适配器不得破坏 JSON 业务结构、表格结构或文档格式；无法安全保留结构时必须返回明确警告并要求人工确认。
- 文件输出路径必须由 Rust 根据受控输入生成；禁止直接使用用户提供文件名拼接任意目录或覆盖原文件。
- 文档元数据中的 SHA-256 只用于文件关联和完整性校验；不得把它当作加密、认证或密钥派生方案。
- `.dlib` 必须使用随机 AES-256-GCM 数据密钥；数据密钥使用 `.p12` 证书中的 RSA 公钥以 OAEP-SHA-256 包装。证书密码只保护 `.p12`，不得直接作为数据加密密钥。
- 加密实现必须使用成熟密码库和系统安全随机源；禁止自定义密码算法、固定 nonce、复用数据密钥或在日志中输出密钥材料。
- 模型下载必须经受控 Provider、临时文件、断点续传和完整性校验后才能进入模型目录；未完成或校验失败的文件不得被加载。
- 本地模型注册必须检查 GGUF magic、文件可读性和 SHA-256；未通过检查的文件不得写入模型清单。
- “支持 GGUF”必须经过架构、元数据、大小和运行时兼容性探测；不得假设所有 GGUF 都能推理或训练。
- LoRA 训练必须固定基础模型 SHA-256、数据集版本和训练配置；训练开始后不得静默更换模型或数据集。
- 暂停训练必须通过检查点实现；训练产物必须记录来源、参数、基础模型和数据集指纹，未经评估不得自动替换当前推理模型。
- 模型评估必须使用与训练集隔离的测试集，至少报告 precision、recall、F1、误报和漏报；不得只用训练损失决定模型晋级。
- 规则与 AI 冲突时保留来源和置信度，统一进入人工审核；任何自动合并策略不得绕过用户确认。
- Rust 脱敏核心只接受已确认区间；必须校验 UTF-8 边界、区间重叠和范围，不得直接信任前端传入的偏移。
- 脱敏完成后必须先持久化 mapping，再写入 history 摘要；任一持久化失败都不得返回“已完成”状态。
- Tauri 迁移采用增量方式：先建立可构建骨架和 DTO，再迁移存储、适配器、脱敏和模型能力；未经确认不得删除旧运行链路。
- 新增依赖后必须同步更新锁文件；若网络或缓存不可用导致无法生成锁文件，必须明确记录并在提交前补齐，不得手工伪造依赖版本或完整性字段。
- 格式转换输出必须经过文件签名、非空和内容抽样校验；禁止以占位文件或虚构成功状态代替真实转换结果。
- 外部网络下载不得在 UI 线程执行阻塞调用；必须提供可见进度、失败状态和可恢复的临时文件策略。
- 长文档复核视图必须保证检测列表、原始内容和脱敏内容的滚动与 hover 联动，关联内容不可见时应自动定位到可视区域。
- 三面板滚动默认独立，不得通过 scroll 事件强制联动；同步定位只允许由 hover 触发。
- 本地模型只有通过 GGUF 校验并登记后才能被应用；模型切换必须由用户显式点击“应用模型”完成。
- 已应用 GGUF 模型必须通过 Candle 实际执行本地推理；不得以模型存在、下载完成或启用状态替代推理结果。
- AI 推理只能返回候选区间和置信度；候选必须进入人工审核，禁止模型直接生成或覆盖脱敏文件。
- Vue 调用 Tauri 必须经过统一 bridge，并保留浏览器兼容判断；页面不得直接依赖内部 API 或绕过 command DTO。
- 桌面测试入口只能使用虚构数据；测试选区必须明确使用 Rust UTF-8 字节偏移，不能把 JavaScript UTF-16 偏移直接传给 Rust。
- 格式能力必须通过 capability 查询暴露；未实现的 DOCX/XLSX/PDF 适配器必须明确返回待接入状态，不得误报可用。
- 模型和任务状态必须使用统一可序列化类型；具体下载、推理和训练后端通过接口隔离，不得在 command 中混入长时间阻塞实现。
# 2026-08-28 实现补充

- `src-tauri` 必须保留可通过 `cargo check` 的 RGBA 图标资源；Rust 构建产物目录不得提交。
# 2026-08-28 开发服务状态

- Tauri CLI 已加入依赖并锁定；运行前端构建后使用 `npm run tauri:dev` 启动桌面测试。
- 首次 Rust 构建较慢属于依赖编译正常现象，未使用类型警告在功能接入后处理。
- 跨 Vue 与 Rust 传递文本选区时必须明确 UTF-8 字节偏移协议，测试不得使用未经验证的固定偏移。
- 模型和任务能力必须经 `src/api/tauriBridge.js` 统一桥接；界面使用任务事件，不得直接访问本地模型路径或临时文件。
- 正式脱敏页接入 Rust 时必须把 JavaScript 字符索引转换为 UTF-8 字节偏移，并保留浏览器旧链路回退。
- 还原页接入 Rust 时必须同时传递脱敏正文和映射 JSON，禁止依赖目标设备本地历史作为跨设备凭据。
- 部分还原必须保留无法匹配的标记并通过结构化结果报告，不得静默删除未知标记。
- 阶段验收以 Rust 测试、Vue 构建、Vite HTTP 200 和 Tauri 进程运行作为直接证据；复杂格式与真实模型推理不得冒充已完成。
- 双栏预览中原始栏必须只读，脱敏栏才允许选区操作；标记识别必须兼容随机不可读 ID。
- 新架构桌面包必须通过 `npm run tauri:build` 生成；`desktop:dist` 仅用于旧 Electron/FastAPI 兼容包，不得作为新架构验收依据。
- 新架构打包验收必须检查 `.app/.dmg` 来自 `src-tauri/target/release/bundle`，并确认界面包含原始/脱敏双栏预览。
- 脱敏页空状态也必须保留原始/脱敏双栏结构，不得以单一预览占位替代目标布局。
- Tauri 包必须能在无 FastAPI 服务时完成文本/PDF 基础检测；兼容端后端调用不得泄漏到 Tauri 页面。
- Tauri 本地检测生成的标记必须为随机不可读 ID，不得复用包含敏感类型名称的旧占位符。
- 历史上传、测试样本、旧构建和旧发布产物统一可恢复地归档到 `trash/`，该目录不得提交；源码和兼容链路不得因此删除。
- Tauri 打包配置必须通过 `beforeBuildCommand` 生成最新 `dist/`，避免源码与安装包前端资源版本不一致。
- 选区悬浮菜单点击外部空白必须关闭；菜单内部操作不能被外部关闭监听拦截。
- 设置页必须提供 AI 默认关闭开关、GGUF 来源选择、下载入口和本地模型登记；模型不得打入 Tauri 安装包。
# 2026-08-28 模型容量显示

- 推荐 GGUF 模型必须标注预计占用，并明确量化版本可能导致实际大小变化；已登记模型显示校验后的实际大小。
# 2026-08-28 打包测试记录

- 用户要求重新打包时必须执行 `npm run tauri:build`，并启动新生成的 `.app` 做烟测；不得只复用旧 bundle。
# 2026-08-28 GitHub Actions 打包

- Tauri 自动打包工作流必须覆盖 macOS ARM64、macOS Intel 和 Windows x64，并使用锁文件安装依赖。
- CI 产物仅上传构建包，不上传历史数据、模型文件、映射表或证书材料。
- 桌面发布由 `v*` tag 触发；Windows MSI、macOS ARM64 DMG、macOS Intel DMG 必须同时上传到同名 GitHub Release。
- Windows CI 默认生成 NSIS 安装包；若 MSI/WiX 在托管 runner 失败，不得将该失败误判为前端或 Rust 编译失败。
- 模型下载入口点击后必须在对应卡片内显示独立进度条和状态。
- 多格式烟测必须使用 `trash/` 中虚构样本，并区分上传/预览验证与结构化脱敏端到端验证结果。
- 版本弹窗的检查更新、更新版本、前往仓库必须分别连接到真实检查状态、发布页和仓库地址，不得使用无动作占位按钮。
- 拖拽上传必须同时处理 `dragenter`、`dragover`、`dragleave`、`drop`；后端不可用时复杂格式不得静默清除用户已选择的文件。
- 本地重新打包后必须启动最新 `.app`，并将包路径和待验证范围记录到 `docs/context.md` 与 `docs/smoke_logs.md`。
- 复杂格式能力不足时必须使用信息提示，明确区分“文件已加入”和“结构化检测不可用”，不得使用错误样式制造歧义。
- 文件转换下载必须校验 Blob 非空，并延迟释放 Object URL；不得在触发下载后立即撤销 URL。
- 每次转换下载修复后重新打包，启动最新 `.app` 进行人工验证。
- Word、Excel、PDF 适配器依赖必须随 Tauri Rust 构建链发布；依赖安装完成不等于适配器业务已接入，能力状态必须如实反映。
- 用户测试协作流程：每次代码修改后必须先完成本地测试、Tauri 打包并启动最新应用交给用户验证；在用户明确反馈测试完成前，不得执行仓库 push。用户确认后再提交并推送。
- DOCX 读取修复后必须本地打包启动并交由用户验证，用户确认前不 push。
- 脱敏页文件上传成功后应自动折叠上传卡片，保留展开入口并优先展示检测结果。
- 检测结果卡片与左右预览中的对应片段必须通过稳定 detection ID 联动 hover，高亮不得改变原始只读和右侧选区约束。
- 检测联动 hover 必须支持检测卡片、原始预览片段和脱敏预览片段任一方向触发。
- Hugging Face 模型下载默认使用 `hf-mirror.com`；未核实的模型地址必须禁用下载，不得伪造直链。
- 设置页推荐模型必须基于真实仓库和真实 GGUF 文件核验；小于 1B 的模型优先用于本地候选检测，找不到真实地址的模型不得展示为可下载。
- 历史记录未保存复杂格式的真实文件字节时，下载入口必须明确提示无法重建；禁止根据预览文本伪造 DOCX、XLSX 或 PDF 文件。
- 所有下载入口必须在系统接收下载后显示成功弹窗，并在生成、校验或提交失败时显示失败弹窗，不得静默结束。
- 原始预览与脱敏预览中同一检测项必须同时使用红色双波浪线标识，且不得影响现有 hover 联动、定位或选区操作。
- 所有用户文件上传、开始执行和“重新开始”操作必须先显示统一的应用内二次确认；取消确认不得读取文件、启动任务或清空当前状态，不得依赖 WebView 原生 `window.confirm`。
- GitHub Release 由 `v*` tag 触发，必须同时构建 macOS ARM64 DMG、macOS Intel DMG、Windows x64 MSI 与 Linux x64 DEB；发布文件以 `local_desens_<平台>.<格式>` 命名，架构后缀用于避免同一 Release 的双 DMG 覆盖，不得把“系统_架构”等模板字样写入实际包名。
- DOCX/XLSX 脱敏与还原必须直接改写原始 OOXML ZIP 包并校验确有替换；浏览器兼容模式也不得回退到未打包的服务生成占位文件。
- JSON、TXT、CSV、Markdown 下载不得向业务正文插入说明文字；脱敏关联信息使用同名 `.desens-meta` 伴随文件。
- 日期规则的月、日多位分支必须优先于单数字分支，避免将 `14` 截断为 `1`。
- 删除历史和规则等破坏性按钮必须先显示确认提示；异步还原必须在文件真正处理完成后才解除加载状态。
- 新生成的历史记录必须把真实脱敏文件 Blob 保存到 IndexedDB，`localStorage` 只保存索引和元数据；历史下载不得根据预览文本重建复杂格式。
- 删除或清空历史时必须同步删除 IndexedDB 中的文件；旧版无文件索引记录应明确标注为旧记录，不得误报可下载。
- Tauri WebView 中不得依赖原生 `window.confirm()` 承载关键删除确认；历史删除和清空必须使用应用内可访问确认弹窗，并在完成后核对元数据与文件存储均已清理。

# 2026-08-31 固定视口布局

- 全局页面使用固定视口的弹性布局，页面壳层不得因内容撑高；内容溢出优先由所属卡片或面板内部滚动承载。
- 安装包使用蓝紫盾牌锁圆角矩形图标，前端 Logo 使用同一图标的透明背景 PNG；图标资源变更后必须经过浏览器烟测和本地 Tauri 打包验证。
- 前端版本号必须从项目版本配置读取；每次发布 tag 前后不得保留写死的旧版本显示。
# 2026-08-31 还原下载与历史检索

- 还原结果下载（Word/Excel/CSV/TXT/Markdown）必须在前端本地生成真实文件；Tauri 页面不得调用未打包的 FastAPI `/api/text-to-*`，避免 SPA 回退把 index.html 当作占位文件下载。
- 本地 OOXML 输出必须通过 ZIP 签名、非空与内容抽样校验；校验失败必须显示失败弹窗，不得以占位文件代替。
- 还原源为 Excel 时，文本类导出必须提取还原后的真实单元格内容，不得导出摘要占位句。
- 脱敏历史面板必须支持按文件名/时间搜索过滤，并按创建时间排序，默认最新在前，可切换最早在前。
- 独立格式转换页面按用户指令移除；源码归档于 `trash/removed-2026-08-31/`，脱敏页内的 PDF→Word 兼容流程保留。
